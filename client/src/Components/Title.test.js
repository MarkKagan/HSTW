import {screen, render, cleanup} from '@testing-library/react';
import {iteratee} from 'lodash';

afterEach(cleanup);

describe("Should provide a description of how the world is doing on render", () => {
  it()
})